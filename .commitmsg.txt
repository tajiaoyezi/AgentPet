feat(m0): scaffold Tauri v2 桌面骨架（三窗口 + 托盘 + 数据目录 + 事件总线 + 日志）

实现 OpenSpec change m0-scaffold-tauri-skeleton：
- Tauri v2 + Vite + React + TS 工程；三窗口（pet-overlay 透明可拖动 / status-panel / settings）。
- 系统托盘（打开面板/设置/退出）+ 关闭到托盘、托盘退出。
- AppState + health command/event 往返；前端各窗口订阅 agentpet://health。
- config/paths（directories 解析 %APPDATA%\AgentPet，幂等建树）+ settings.json 占位；tracing 文件日志（best-effort 降级）。
- event_bus/session/adapters/pet/notify/store 占位模块预留 M1–M6。

验证：npm run build（tsc + vite）与 cargo test（全量编译 + paths/settings 单元测试）通过。
GUI/运行时验收（1.4/3.4/4.6/5.5/6.5）待 npm run tauri dev 人工确认。
