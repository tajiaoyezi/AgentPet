---
description: Git/PR 工作流 — 保护主分支、功能完成必须走 PR、合入前由子 agent 做对抗式审查循环
alwaysApply: true
---

# Git / PR 工作流（强制）

## 主分支保护
- 禁止直接向主分支（默认 `master`）提交或推送代码。
- 禁止 `git push origin master`，禁止对主分支 force-push。
- 所有开发在功能分支进行；分支名建议与 OpenSpec change 一致，例如 `m0-scaffold-tauri-skeleton`（或 `feat/...`、`fix/...`）。

## 完成功能 → 提 PR
完成一项功能需求（通常 = 一个 OpenSpec change 的 tasks 全部完成并自验）后：
- 推送功能分支：`git push -u origin <branch>`
- 开 PR：`gh pr create --base master --head <branch> --title "..." --body "..."`

## 对抗式审查循环（合入前必须）
提 PR 后，**必须**派子 agent 对 PR 改动做**对抗式审查**，通过后才可合入：

1. 用 Task 工具启动审查子 agent（`bugbot` 或 `security-review`，或 `generalPurpose` 以红队心态审查 `git diff master...<branch>`），以 readonly 运行。
2. 审查心态：默认假设代码有问题，主动找 bug、回归、边界条件、安全隐患、以及与 proposal/specs 不符之处。
3. 判定：
   - **无阻断问题** → 允许合入：`gh pr merge <pr> --squash --delete-branch`
   - **存在问题** → 在同一分支修复并 `git push`，然后**重新执行步骤 1** 再审。
4. 循环 1–3，直到审查通过；只有通过后才允许合并。

## 红线
- 审查未通过不得合入。
- 不得跳过对抗式审查直接合并。
- 不得用直推主分支绕过 PR 流程。
